# Connect IQ build artifacts
bin/
*.prg
*.iq
*.debug.xml

# Developer signing keys — never commit
developer_key.pem
developer_key.der
*.der
*.pem

# Generated golden vectors are committed; the JDK/oracle used to regenerate them is not.

# Personal beta build (scripts/beta-build.sh) — unique per builder, never committed
.beta-app-id
manifest-beta-local.xml
beta-local.jungle
barrels/*.barrel
.DS_Store
