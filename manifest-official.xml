<?xml version="1.0" encoding="UTF-8"?>
<!-- faBolus Garmin watch app — OFFICIAL store listing. Identical to the beta (manifest.xml, same
     source + resources) except a distinct app id + the official name. Built via official.jungle.
     NOTE: this id differs from the one the iOS ConnectIQ bridge targets
     (GarminRemoteBridge.watchAppUUID = a1b2c3d4…, the beta). For the iPhone app to pair with this
     OFFICIAL Garmin app, point the bridge at this id (or have it target both). -->
<iq:manifest xmlns:iq="http://www.garmin.com/xml/connectiq" version="3">
    <iq:application entry="FaBolusApp"
                    id="ded131ecb69d46493650153aef623be6"
                    launcherIcon="@Drawables.LauncherIcon"
                    name="@Strings.AppName"
                    type="watch-app"
                    minSdkVersion="3.3.0">
        <iq:products>
            <!-- Touchscreen watches. venu3s is hardware-validated (the sole supported device). -->
            <iq:product id="venu3s"/>
        </iq:products>
        <iq:permissions>
            <iq:uses-permission id="Communications"/>
            <iq:uses-permission id="ComplicationPublisher"/>
            <iq:uses-permission id="Background"/>
            <!-- Raw accel+gyro streaming for the eating-detection relay (EatingSenseKit barrel). -->
            <iq:uses-permission id="Sensor"/>
            <!-- CX-G-06 (13-07): Toybox.Notifications.showNotification(), called from BgServiceDelegate
                 so a critical pump alert is not silent on the wrist while the app is suspended/closed
                 (see 13-CXG06-FEASIBILITY.md). Mirrors manifest.xml. -->
            <iq:uses-permission id="Notifications"/>
        </iq:permissions>
        <iq:languages>
            <iq:language>eng</iq:language>
        </iq:languages>
        <!-- Reusable wrist eating-sensing library; built from the faBolusNudge SDK. -->
        <iq:barrels>
            <iq:depends name="EatingSenseKit" version="1.0.0"/>
        </iq:barrels>
    </iq:application>
</iq:manifest>
